# 둘 이상의 반복 가능한 자료형을 인자로받아
# 동일 위치의 항목을 묶어 튜플을 항목으로 구성하는 zip객체를
# return하는 함수

# 이때 인자로 전달된 객체는 동일 자료형이면서, 항목의 개수가 같아야함

data1 = [1, 2, 3]
data2 = [4, 5, 6]
data3 = ['a', 'b', 'c']

print(list(zip(data1, data2, data3)))
# $ [(1, 4, 'a'), (2, 5, 'b'), (3, 6, 'c')] 