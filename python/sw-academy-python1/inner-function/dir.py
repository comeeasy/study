# dir

# -help 같은 함수네

# 인자로 전달된 객체가 가지고 있는 변수, 메소드와 같은
# 속성 정보를 리스트 객체로 return, 인자를 전달하지 않고 호출하면
# 현재 local scope에 대한 정보를 리스트 객체로 return

#print(dir())

data = "helle"
print(dir(data.istitle()))
print(data.istitle())