
	< Branch >

	report--report2--report3--report4--------report5
						    \				/
							  report-client

	위와 같이 여러 버전들에 대한 파생버전들이 필요할 때가 있고
	fast-forward 방식처럼 개발하는 과정에서 개발하고 새롭게 업데이트를 버전관리로 할 수 있다.

	이렇게 새로운 파생 버전을 만드는 것을 "Branch를 만든다." 라고 한다.

	* git은 모든 것을 branch의 개념으로 이해하기 때문에 매우 중요하다.
	
	$ git commit -am "message"
		- commit하기 전에 add하고 "message"라는 주석으로 commit 한다 라는 프롬프트

	$ git branch
		- 현재 생성된 branch를 보여준다.
	$ git branch <branch 이름>
		- 해당 이름으로 새로운 branch를 만든다.
	$ git chechout < branch 이름 >
		- 해당 branch로 checkout 하는 것이고 해당 branch의 commit을 다루겠다. 라는 의미 
