#include "./header/adj_matrix.h"

/*
    usage of non recursive dfs of adjacency matrix

int main() {
    matrixGraph g;

    g.get_adj_matrix("./source/graph.txt");
    g.show_graph();

    g.dfs_adj_matrix_nonrecur();

    return 0;
}
*/
/*
    usage of count_components()

int main() {
    matrixGraph g;

    g.get_adj_matrix("./source/graph.txt");
    g.show_graph();

    g.count_components();

    return 0;
}
*/
