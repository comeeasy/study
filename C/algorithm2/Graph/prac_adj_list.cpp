#include "./header/adj_list.h"

/*
int main() {
    listGraph g;

    g.input_adj_list("./source/graph.txt");
    g.show_graph();

    g.bfs_adj_list_nonrecur();

    return 0;
}
*/

// prac of pfs
int main() {
    weightedListGraph wg;

    wg.input_data("./source/weighted_graph.txt");
    //wg.show_graph();
    //wg.pfs_adjlist();

    return 0;
}
